<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:background="@color/black"
    >

    <!-- Horizontal Button Menu at the top -->
    <LinearLayout
        android:id="@+id/buttonMenu"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:padding="16dp"
        android:layout_alignParentTop="true"
        >

        <!-- Icon for Page 1 -->
        <ImageButton
            android:id="@+id/buttonPage1"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:scaleType="centerInside"
            android:background="@null"
            android:contentDescription="Colour Picker Tab"
            android:src="@drawable/colorpicker"/>

        <!-- Icon for Page 2 -->
        <ImageButton
            android:id="@+id/buttonPage2"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:scaleType="centerInside"
            android:background="@null"
            android:contentDescription="Calendar Tab"
            android:src="@drawable/calendar"
            android:layout_marginStart="64dp"/>

        <!-- Icon for Page 3 -->
        <ImageButton
            android:id="@+id/buttonPage3"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:scaleType="centerInside"
            android:background="@null"
            android:contentDescription="Analysis Tab"
            android:src="@drawable/analysis"
            android:layout_marginStart="64dp"/>
    </LinearLayout>

    <!-- ViewPager2 should be positioned below the buttons -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="90dp"
        android:layout_below="@id/buttonMenu" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
